# Componentes shadcn/ui

- Ultima actualizacion: Marzo 2026
- Owner: Frontend

Componentes activos en `components/ui/`: `accordion`, `badge`, `breadcrumb`, `button`, `card`, `input`, `label`, `separator`, `sheet`, `sidebar`, `skeleton`, `sonner`, `table`, `tooltip`, `vortex`.

## Regla

- Evitar cambios extensivos en componentes base generados.
- Para personalizacion, preferir wrappers o clases adicionales con `cn()`.
